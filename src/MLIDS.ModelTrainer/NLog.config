<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<nlog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
      autoReload="true"
      throwExceptions="false" 
      internalLogLevel="Warn" 
      internalLogFile="MLIDS.ModelTrainer.log">
  <variable name="DefaultLayout" value="${longdate} | ${level:uppercase=true:padding=5} | ${callsite} | ${message} | ${exception:format=type,tostring}"/>
  <variable name="AppName" value="MLIDS.ModelTrainer"/>
  <targets>
    <target name="log" xsi:type="File"
            fileName="MLIDS.ModelTrainer.log"
		    archiveFileName="log_archives/MLIDS.ModelTrainer_{#}.log"
            archiveEvery="Day"
            archiveNumbering="DateAndSequence"
            archiveDateFormat="yyyyMMdd"
            maxArchiveFiles="30"
            archiveAboveSize="10000000" />
  </targets>
  <rules>
    <logger name="*" minlevel="Error" writeTo="log"/>
  </rules>
</nlog>